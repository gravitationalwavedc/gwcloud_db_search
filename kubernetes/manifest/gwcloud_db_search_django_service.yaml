apiVersion: v1
kind: Service
metadata:
  labels:
    name: gwcloud-db-search-django
  name: gwcloud-db-search-django
  namespace: gwcloud
spec:
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    name: gwcloud-db-search-django
  sessionAffinity: None
  type: ClusterIP
